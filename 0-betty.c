#include"main.h"
/**
 * add_numbers - function that adds two numbers
 * @a: parameter
 * @b: parameter
 * Return: 0 on success
 */
int add_numbers(int a, int b)
{
	return (x + y);
}
int main(void);
{
	int x = 5;
	int y = 10;

	int sum = add_numbers(x, y);

	printf("the total of sum %d and %d is %d\n", x, y, sum);

	return (0);
}
